import { FC } from "react";

interface LiquidationProps {}

const Liquidation: FC<LiquidationProps> = ({}) => {
  return <div className="font-semibold text-orange-500">Liquidation page</div>;
};

export default Liquidation;
